type Dentist
@model {
  id: ID!
  firstName: String
  lastName: String
  phone: String
  qualifications: String
  bio: String
  website: String
  city: String
  street: String
  postIndex: String
  email: String!
  lat: String
  lng: String
  registered: Boolean
  sub: String
  createAt: String
}

type Service @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
}

type Practice @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
}